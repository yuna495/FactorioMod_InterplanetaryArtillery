# Monolith グラフィック実装仕様書

**Project:** Interplanetary Artillery
**Weapon:** Monolith-class Interplanetary Cannon
**Japanese name:** モノリス級惑星間砲

## 1. 目的

この仕様書は、Monolith-class Interplanetary Cannon のゲーム内グラフィック実装に必要なアセット構成を定義する。

目的は以下の通り。

1. 必要な画像パーツを明確にする
2. Blenderモデル制作対象を明確にする
3. Factorio用スプライト書き出し単位を明確にする
4. CodexがBlender作業・画像出力・prototype組み込みを進められる状態にする

本仕様書は**最終品質のアート完成**を要求するものではない。

まずは**ゲーム内で成立する実装用スプライト構成**を定める。

---

## 2. グラフィック全体構成

Monolithは大きく以下のパーツで構成する。

1. **Foundation**
2. **Foundation Production Overlay**（任意）
3. **Foundation Charge Overlay**（任意）
4. **Turret Base**（旋回基部）
5. **Barrel**（砲身）
6. **Muzzle Flash**（砲口閃光）
7. **Projectile Visual**（砲弾の視覚表現）
8. **Impact Marker / Countdown Marker**（必要なら別途）

今回の主要対象は、

* Foundation
* Turret Base
* Barrel
* Muzzle Flash

とする。

Production Overlay / Charge Overlayは、必要になった時点で追加する。

---

## 3. パーツごとの役割

### 3.1 Foundation

役割:

* 15×15の基礎施設本体
* 地面・蓄電・装填機構・支持構造を含む
* 旋回しない
* 仰角変化しない
* 発射時にも原則として本体形状は変化しない
* 砲弾製造設備を含む
* 惑星間砲撃用の蓄電・充電設備を含む想定

必要スプライト:

* **固定1枚**

Foundation本体は常に固定スプライトとする。

砲弾製造中の機械動作や惑星間砲撃時の帯電・発光を表現する場合は、Foundation本体とは別のoverlay animationとして追加する。

---

### 3.2 Foundation Production Overlay

役割:

* 砲弾製造中の機械稼働を表現する
* Foundation本体の上に重ねて表示する
* 必須ではない

候補:

* 搬送機構
* 装填ハッチ
* 回転機械
* ピストン
* 小型クレーン
* 工場内部の発光

初期版では未実装でもよい。

---

### 3.3 Foundation Charge Overlay

役割:

* 惑星間砲撃前の大規模充電を表現する
* Foundation本体の上に重ねて表示する
* 必須ではない

候補:

* コンデンサ発光
* 放電
* 電弧
* 発光配管
* 蓄電設備の段階的点灯

初期版では未実装でもよい。

---

### 3.4 Turret Base

役割:

* Foundation中央に設置される砲の旋回基部
* 水平方向へ旋回する
* Barrelの支持構造を含む
* Blender内ではBarrelと別オブジェクトを維持する。画像は両者をUpper Assemblyとして同時出力する

必要スプライト:

* 単独画像は出力せず、Barrelと合わせて **24方向 × 5仰角**

Turret Baseは15度刻みで旋回表現を持つ。

---

### 3.5 Barrel

役割:

* 砲身本体
* Turret Baseと合わせてUpper Assemblyとして表示
* 水平方向へTurret Baseと同期して旋回
* 仰角が変化する
* 発射時に反動する

必要スプライト:

* **24方向 × 5仰角**

---

### 3.6 Muzzle Flash

役割:

* 発射瞬間の砲口閃光
* 短時間のみ表示
* 砲本体とは別sprite / animationで扱う

初期版では、

* **共通1種類**

を基本とする。

砲口位置・砲身方向に応じて回転・配置できる場合は、方向別画像を作らない。

共通画像では見た目が不自然な場合のみ、方向別差分を追加する。

---

## 4. 実装方針

### 4.1 分離実装

Monolith本体は以下のように分離する。

```txt
Foundation
+ optional Production Overlay
+ optional Charge Overlay
+ Turret Base
+ Barrel
+ Muzzle Flash
```

これにより、

* Foundationは固定
* Turret Baseは水平方向へ旋回
* Barrelは水平方向旋回 + 仰角切替
* 発射時のみMuzzle Flash
* 将来的にFoundation上へ製造・充電演出追加

を可能にする。

---

## 5. 初期実装フェーズ

### Phase 1

* Foundation固定画像
* Upper Assembly（Turret + Barrel）24方向 × 5仰角

### Phase 2

* Barrel反動
* Muzzle Flash

### Phase 3

* Foundation Production Overlay
* Foundation Charge Overlay
* 発射煙
* 発射粉塵
* 細かな補助演出

---

## 6. 方向数

### 6.1 Foundation

* 方向なし
* **固定1枚**

---

### 6.2 Turret Base

方向数:

* **24方向**

1方向あたり:

* **15度**

0～345度まで15度刻みで用意する。

例:

```txt
0°
15°
30°
45°
60°
...
330°
345°
```

8方向のような固定方位名ではなく、角度番号または連番で管理してよい。

---

### 6.3 Barrel

方向数:

* **24方向**

Turret Baseと同じ角度体系を使用する。

Turret BaseとBarrelの水平角は常に一致させる。

---

## 7. 仰角段階

Barrelには5段階の仰角差分を持たせる。

仮の角度:

1. **low** — 0°
2. **low-mid** — 15°
3. **mid** — 30°
4. **high-mid** — 45°
5. **high** — 60°

角度は初期値であり、後から調整可能。

視覚的に各段階の差が明確に分かることを優先する。

必要レンダー数:

```txt
24方向 × 5仰角 = 120レンダー
```

---

## 8. 仰角のゲーム内利用

初期実装では、完全な弾道計算と連動させる必要はない。

当面は例えば以下のような段階選択でもよい。

```txt
same-surface 近距離
→ low

same-surface 中距離
→ low-mid / mid

same-surface 長距離
→ high-mid

interplanetary
→ high
```

将来的に弾道計算や距離ベースの仰角選択へ置き換えられる構造とする。

---

## 9. 発射反動

将来の追加対象。今回のUpper AssemblyはTurretとBarrelを一緒に出力するため、
画像全体の位置シフトではBarrelのみの反動を表現できない。
実装時に専用状態の追加または砲身の分離パスを検討する。今回の出力には含めない。

---

## 10. Muzzle Flash

初期版は簡易でよい。

基本方針:

* 共通1種類
* 短時間表示
* 砲身角度に応じて配置
* 必要なら回転

共通画像方式で不自然な場合のみ、

* 方向別
* 複数フレーム

へ拡張する。

初期段階で24方向分を必須とはしない。

---

## 11. Foundation詳細デザイン要件

Foundationには以下の見た目要素を含める。

* 多角形の砲座基部
* 重厚な支持構造
* 装甲板
* 配管
* 主砲弾製作工場
* 蓄電設備 / コンデンサ風ディテール
* 装填用ハッチ
* 搬入口
* 整備用構造
* Factorio世界らしい工業感

重要:

* **巨大砲の基礎施設に見えること**
* 15×15の大型設備として違和感がないこと
* Cannonが載っていなくても「ただの床」に見えないこと
* 巨大な製造設備・兵器基盤として成立すること

---

## 12. Turret Base詳細デザイン要件

Turret Baseには以下を含める。

* 旋回砲塔基部
* Barrel支持フレーム
* 仰角調整機構
* 大型軸受
* 油圧 / 電動駆動機構
* 重機械感のある構造

重要:

* Foundationの上に載ったとき一体感があること
* Barrel無しでも「大型砲の旋回基部」に見えること
* 24方向どの角度でもFoundationとの接続が自然なこと
* Barrelとの接合軸が全方向・全仰角で一致すること

---

## 13. Barrel詳細デザイン要件

Barrelには以下を含める。

* 非常に長い砲身
* 大口径砲口
* 砲身支持構造
* 後部機構
* 反動機構
* 必要なら補強リングや外装

重要:

* **巨大兵器らしい長さ**
* 細長くても存在感があること
* 仰角差分が明確に見えること
* 24方向どの角度でもシルエットが破綻しないこと

目安:

* Foundation footprintより大幅に長い
* 全長として**30～40 tile級**に見えるシルエット

---

## 14. 色・質感方針

MonolithはFactorio世界観に合わせて以下を基本とする。

* ダークグレー
* 鋼鉄色
* くすんだ工業色
* 使用感のある金属
* 機械油
* 煤
* 焼け
* 一部に黄色 / オレンジの注意色
* 発光要素は控えめ

避けるもの:

* 派手すぎるSFネオン
* 真っ白で清潔すぎる外装
* アニメ調の極端な彩色
* 過剰に未来的でFactorio世界から浮くデザイン

---

## 15. Blender作業対象

最低限以下の3Dモデルを作成する。

### 必須

1. Foundation model
2. Turret Base model
3. Barrel model

### 補助

4. Muzzle Flash用2D素材または簡易エフェクト

### 初期版では不要

* 完全な煙シミュレーション
* 完全な充電アニメーション
* 細密な内部機構
* 被弾破損モデル
* 破壊モデル

---

## 16. Blenderモデル構造

Blender内でも最低限以下を別オブジェクトとして扱う。

```txt
Monolith_Foundation
Monolith_Turret
Monolith_Barrel
```

TurretとBarrelの回転軸・仰角軸は正確に設定する。

Barrelの仰角変更時に、

* 基部から浮く
* 接続部がずれる
* 支持フレームを貫通する

等が起きないようにする。

---

## 17. レンダリング方針

### 17.1 カメラ

* Factorio風の固定斜め視点
* Foundation / Turret / Barrelで同一カメラを使用
* ズーム・焦点距離・角度を固定
* Blender Pythonから再現可能な設定にする

---

### 17.2 出力

* PNG
* 透過背景
* FoundationとUpper Assemblyを分離出力（TurretとBarrelはセット）
* 自動レンダリング可能な構造

---

### 17.3 影

必要に応じて、

* Foundation shadow
* Turret shadow
* Barrel shadow

を別出力する。

初期版では簡易影でもよい。

後からshadow spriteを分離できる構造を維持する。

---

## 18. 自動レンダリング

最新の `art/blender/monolith_v2_symmetry.blend` を入力とし、
`render_monolith.py` が出力用 `monolith_render.blend` と透過RGBA PNGを生成する。
元モデルは更新しない。TurretのZ旋回とBarrelのX仰角を定値キーフレームで管理する。

```txt
for elevation in [0, 15, 30, 45, 60]:
    for direction in range(24):
        frame = elevation_index * 24 + direction + 1
        set turret yaw = direction * 15 degrees
        set barrel elevation
        render Upper Assembly (Turret + Barrel)
```

Foundationは固定1枚、Upper Assemblyは120枚、合計121枚。
フレーム1–24 / 25–48 / 49–72 / 73–96 / 97–120が各仰角ブロックに対応する。
方向00はモデル+Y、正の旋回は+Z軸周り（90度は−X）。

カメラは45度俯角の正投影。全状態の境界をまとめて求め、共通の位置・画角・
768×768キャンバスで出力する。画像ごとのトリミングは行わない。
材質・照明は入力モデルから継承し、背景とスタジオ床は出力しない。
方向数・仰角リスト・解像度・サンプル数・出力先は実行引数で変更できる。
`manifest.json` に各画像の状態、画角、寸法と透過範囲を保存する。

先に `--preview` でFoundationと代表6状態を確認し、その後全出力する。
再実行では同じ出力名を使用する。今回の対象にprototype組み込みやエフェクトは含めない。

---

## 19. ファイル構成案

```txt
graphics/
  entity/
    monolith/
      foundation/
        monolith-foundation.png

      upper/
        low/
          monolith-upper-low-00.png
          ...
          monolith-upper-low-23.png

        low-mid/
          monolith-upper-low-mid-00.png
          ...
          monolith-upper-low-mid-23.png

        mid/
          ...

        high-mid/
          ...

        high/
          ...

      muzzle-flash/
        monolith-muzzle-flash.png
```

番号と実角度の対応を別途明文化すること。

例:

```txt
00 = 0°
01 = 15°
02 = 30°
...
23 = 345°
```

---

## 20. Sprite Sheet

最終的なFactorio組み込みでは、必要に応じて個別PNGをsprite sheetへ統合してよい。

Blenderからの中間出力は個別PNGでも構わない。

Codex側で、

* trimming
* padding
* sheet結合
* metadata整理

を自動化してよい。

ただし画質劣化や座標ずれを起こさないこと。

---

## 21. ゲーム内実装優先度

### 優先度A

* Foundation
* Upper Assembly（Turret + Barrel）24方向 × 5仰角

### 優先度B

* Barrel recoil
* Muzzle Flash

### 優先度C

* Foundation Production Overlay
* Foundation Charge Overlay
* 発射煙
* 発射粉塵
* GUIアイコン類の高品質化

---

## 22. Codexへの作業指示の前提

現在のBlender出力工程は以下とする。

1. 本仕様書を読む
2. 最新の `monolith_v2_symmetry.blend` と既存の確認画像を確認する
3. 既存モデルの形状・材質・親子構造を維持する
4. Factorio向け固定カメラを設定する
5. Blender Pythonで24方向 × 5仰角のフレーム管理を自動化する
6. Foundationと代表6状態のUpper Assemblyをプレビューする
7. Foundation 1枚とUpper Assembly 120枚を透過PNGで出力する
8. 透過・画角・姿勢変更・再生成を確認する

sprite sheet化、prototype組み込み、ゲーム内確認は後続工程とする。

Codexにはゼロから芸術的なコンセプトを考えさせるのではなく、

**本仕様書および別途与えるコンセプト画像を基準として、3Dモデル・レンダリング・Factorio実装を行わせる。**

---

## 23. コンセプト画像の扱い

Blenderモデル制作時は、別途用意したMonolithのグラフィックイメージをデザイン基準として使用する。

コンセプト画像は、

* シルエット
* 比率
* 砲身長
* Foundationの構成
* Turret形状
* 色
* 工業的雰囲気

の参考とする。

画像を完全にピクセル単位で再現する必要はない。

Factorioのゲーム画面上で成立することを優先する。

---

## 24. 今回まだ不要なもの

以下は初期グラフィック実装対象外とする。

* 最終的な高品質テクスチャ仕上げ
* 64方向旋回
* 滑らかな連続仰角
* 完全な発射アニメーション
* 完全な充電アニメーション
* 被弾差分
* 破壊差分
* 惑星間砲撃専用弾頭モデル
* 細かな内部構造の見せ込み
* 最終的な発射煙
* 最終的な粉塵
* 最終的な照明演出

---

## 25. 最終決定事項

現時点のMonolith本体グラフィック構成は以下とする。

### Foundation

* 固定1枚
* 旋回なし
* 仰角なし
* 製造・充電演出は別overlayとして将来追加可能

### Upper Assembly（Turret + Barrel）

* 24方向、15度刻み
* 5仰角
* 両者を一緒に合計120レンダー

### 発射反動

* 将来の追加対象。Upper Assemblyとは別途、表現方式を検討する

### Muzzle Flash

* 初期版は共通1種類
* 必要なら後から方向差分追加

### Blender出力

* Blender Pythonによる自動レンダリングを前提
* 個別PNGから開始してよい
* 必要に応じてsprite sheetへ統合

この構成を初期グラフィック実装の基準とする。
